# W02 plan

Run repository-full CI, state consistency, upgrade compatibility, fixture regression, live bulk smoke and secret audit. Merge only after all required gates pass, then confirm exact-merge Post-Merge finalization and the automatic financial request artifact.
